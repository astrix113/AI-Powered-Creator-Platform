/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: ['ik.imagekit.io', 'images.unsplash.com', "img.clerk.com"],
  },
};

export default nextConfig;
